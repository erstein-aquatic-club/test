{
  "name": "repatriement-desktop",
  "private": true,
  "version": "0.1.0",
  "main": "main.cjs",
  "scripts": {
    "start": "VITE_DEV_SERVER_URL=http://localhost:4173 electron .",
    "build": "npm --prefix ../pwa run build && electron-builder --win --config electron-builder.json"
  },
  "devDependencies": {
    "electron": "^31.4.0",
    "electron-builder": "^24.13.3"
  }
}
